# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12          # file uploads
pydantic==2.9.2
pydantic-settings==2.6.0

# Database
sqlalchemy==2.0.36
alembic==1.13.3
psycopg2-binary==2.9.10           # postgres driver
# For dev you can fall back to sqlite — no driver needed

# Caching / queue
redis==5.1.1
celery==5.4.0

# Market data
yfinance==0.2.48
pandas==2.2.3
numpy==2.1.2

# PDF processing
pdfplumber==0.11.4
pypdf==5.1.0
# camelot-py[cv]==0.11.0          # uncomment if you need table extraction (heavy deps)

# AI
anthropic==0.39.0

# RAG / embeddings (optional — enable when you build the RAG endpoint)
# chromadb==0.5.18
# sentence-transformers==3.2.1

# Auth (optional)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Dev
pytest==8.3.3
httpx==0.27.2
ruff==0.7.1
